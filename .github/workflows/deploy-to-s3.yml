name: Tag deploy to S3
on:
  push:
    branches: 
      - dev
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout # 步骤名称
        uses: actions/checkout@v2
        with: 
          persist-credentials: false

      - name: Install and Build
        run: | # 该步骤要运行的命令，这里就是部署react 的命令
          npm install
          npm run-script build

      - name: Configure AWS credentials from Test account
        uses: aws-actions/configure-aws-credentials@v1
        with: 
          aws-access-key-id: AKIA2VDFLWRT3EJ5GOGP
          aws-secret-access-key: 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
          aws-region: us-east-2
      
      - name: Copy files to the test website with the AWS CLI
        run: aws s3 sync ./build s3://my-xiaowei-hosted-content

